import React from 'react';
import { Typography } from '@mui/material';

const AgentDetail = () => {
  return (
    <Typography variant="h4">
      Agent Detail Page (Coming Soon)
    </Typography>
  );
};

export default AgentDetail;